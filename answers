1. They set the parameters for Git, such as naming the default branch as "main" and using NotePad as Git's default editor.

2. The command line is fast and efficient as it does not really have any fancy user interfaces that may serve as a hindrance. It also gives programmers more control and flexibility.

3. I typed "cd tmp", and when I entered "pwd" again, the output was "/tmp"

4. It is a markdown file and it has 3 sections. The preview is my repository name and description. The code section is also my file name and description then the blame section is the commits and edits I did.
